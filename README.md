# 📄 Interactive Portfolio: Personal Statement

`Personal Statement`는 단순히 과거의 결과물을 나열하는 정적인 포트폴리오를 넘어, **Three.js**를 활용해 여러 프로젝트를 하나의 일관된 세계관으로 묶어낸 **인터랙티브 프로젝트 모음집**입니다.

🚀 **서비스 링크**
* **Live Demo**: [데모링크](https://gyo50.github.io/Personal-Statement/)
* **Environment**: React 19, Three.js, Framer Motion

---

## 🛠 기술 스택
* **Framework**: React 19 (v7 Router)
* **Build Tool**: **Vite**
* **Graphics**: **Three.js**, **@react-three/fiber**, **@react-three/drei**
* **Animation**: **Framer Motion**
* **Styling**: Tailwind CSS (v4)
* **Deployment**: GitHub Pages

---

## 👨‍💻 주요 특징

### 1. Three.js 기반의 3D 공간 탐색
* Three.js 캔버스를 활용해 웹 페이지를 입체적인 공간으로 구성했습니다.
* 페이지 진입 시 카메라의 회전(Rotation)과 위치(Position) 값을 조절하여, 종이가 회전하며 펼쳐지는 듯한 역동적인 시점 변화와 몰입감을 구현했습니다.

### 2. 프로젝트별 테마 커스텀 커서 시스템
* 각 프로젝트(Diptyque, SCM 등)의 아이덴티티에 맞춘 독자적인 마우스 인터랙션을 설계했습니다.
* 마우스 이동 좌표를 실시간으로 추적하여 텍스트 회전 애니메이션(Diptyque) 및 네온 파동 효과(SCM)를 적용해 사용자 경험(UX)을 극대화했습니다.

### 3. 컴포넌트 기반 데이터 관리 및 확장성
* 프로젝트 상세 정보를 독립적인 모듈(JS)로 분리하여 유지보수성과 확장성을 높였습니다.
* `DetailCard`와 `Modal` 공통 컴포넌트를 설계하여 프로젝트별로 일관된 디자인 시스템을 유지하면서도 각 테마 컬러를 유연하게 반영할 수 있게 구현했습니다.

---

## 🔍 핵심 트러블슈팅

### 1. 3D 캔버스와 DOM 요소 간의 시각적 동기화 및 뎁스 제어
* **문제**: Three.js로 구현한 3D 배경 위로 HTML 요소가 겹쳐질 때, 특정 인터랙션에서 3D 오브젝트와 텍스트의 레이어 순서가 엉키거나 클릭 이벤트가 간섭받는 현상이 발생했습니다.
* **해결**: `GhostCanvas` 구조를 설계하여 3D 렌더링 영역과 인터랙티브 UI 영역을 명확히 분리했습니다. `pointer-events-none`과 `pointer-events-auto` 속성을 적절히 배분하여, 화려한 3D 애니메이션을 감상하면서도 실제 UI 조작에는 전혀 방해가 없도록 뎁스(Z-index) 체계를 구축했습니다.

### 2. 고해상도 애니메이션 시 마우스 트래킹 지연(Latency) 최적화
* **문제**: React의 상태 업데이트(`setState`)만으로 마우스 좌표를 추적할 경우, 리렌더링 주기에 따른 미세한 시차로 인해 커스텀 커서가 마우스 포인터를 한 박자 늦게 따라오는 이질감이 발생했습니다.
* **해결**: CSS 변수(`--x`, `--y`)와 `will-change` 속성을 활용해 브라우저 GPU 가속을 유도하고 리렌더링 부하를 줄였습니다. 위치 값 변경에는 `transition`을 제외해 마우스 움직임에 즉각 반응하게 하고, 크기 변화(Scale)에만 부드러운 애니메이션을 부여하여 최적의 조작감을 구현했습니다.

### 3. 최신 기술 스택(Tailwind v4 & React 19) 도입 시 호환성 해결
* **문제**: 프로젝트 초기 설정 시 최신 버전인 Tailwind CSS v4와 React 19의 바뀐 설정 체계로 인해 기존 Three.js 관련 라이브러리들과 스타일 충돌 및 설정 오류가 발생했습니다.
* **해결**: PostCSS 설정 파일을 직접 커스텀하고 v4의 새로운 지시어 체계를 학습하여 적용했습니다. 이를 통해 최신 프레임워크가 제공하는 성능 이점(빌드 속도 개선 등)을 확보하면서도 안정적인 인터랙티브 레이아웃을 완성했습니다.

---

## 📂 폴더 구조
```text
src/
├── assets/         # 프로젝트 공통 이미지 및 SVG 소스
├── components/     # 재사용 가능한 UI 컴포넌트
│   ├── common/     # Modal, DetailCard, GhostCanvas 등
│   └── contents/   # 개별 프로젝트 데이터 (diptyque, scm, nexon 등)
├── test/           # Three.js 캔버스 및 애니메이션 실험실
│   ├── components/ # 3D 오브젝트(Paper 등) 및 로직
│   └── test.jsx    # 메인 인터랙티브 레이아웃
└── App.js          # 라우팅 및 전역 상태 관리
```
